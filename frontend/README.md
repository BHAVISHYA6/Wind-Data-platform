# Wind Analytics Dashboard Frontend

React + Vite dashboard shell for wind energy analytics.

## Run locally

```bash
npm install
npm run dev
```

This frontend is intentionally disconnected from APIs for now and provides reusable layout components only.
