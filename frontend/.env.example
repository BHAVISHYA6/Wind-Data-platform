# ── API / WebSocket ───────────────────────────────────────────────
# URL of the backend server (used for REST API calls and Socket.IO connection)
# Local:
#   VITE_API_BASE_URL=http://localhost:5000
# Production (Azure App Service example):
#   VITE_API_BASE_URL=https://your-app.azurewebsites.net
VITE_API_BASE_URL=http://localhost:5000
